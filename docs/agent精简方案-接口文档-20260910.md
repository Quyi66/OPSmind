# KoreOps Agent 通道 · 精简版接口文档（2026-09-10）

> 与 `docs/agent精简方案-设计-20260910.md` 配套。本文是精简后的**完整接口契约**，不是增量；旧接口的去留见 §6 对照表。
> 三类调用方：**前端**（管理面，登录态）、**Agent 客户端**（出站，`X-Agent-Token`）、**executor 连接插件**（内部，`X-Relay-Token`）。

---

## 0. 总览

| 面 | 方法 | 路径 | 调用方 | 鉴权 |
|---|---|---|---|---|
| 管理 | POST | `/api/cmdb/agent/enroll-tokens` | 前端 | 登录态，角色 ADMIN / PRIVUSER / DEVELOPER / FREE |
| 管理 | DELETE | `/api/cmdb/agent/enroll-tokens/{id}` | 前端 | 同上 |
| 管理 | GET | `/api/cmdb/agent/pending?tokenId=` | 前端 | 登录态 |
| 管理 | POST | `/api/cmdb/agent/bind` | 前端 | 角色同 enroll |
| 管理 | POST | `/api/cmdb/agent/unbind` | 前端 | 角色同 enroll |
| 管理 | GET | `/api/cmdb/agent/host-info?hostIds=` | 前端 / secops 内部 | 登录态 |
| 管理 | POST | `/api/cmdb/agent/install` | 前端（主机列表"安装 Agent"） | 角色同 enroll |
| 管理 | POST | `/api/cmdb/agent/install/callback` | workflow 回调 | 登录态（workflow 带运行者令牌） |
| Agent | GET | `/api/agent/i/{token}` | 目标机（一行安装命令） | 路径里的 enrollment token |
| Agent | POST | `/api/agent/register` | Agent | `X-Agent-Token`（enrollment 或 client token） |
| Agent | GET | `/api/agent/poll` | Agent | `X-Agent-Token`（client token） |
| Agent | POST | `/api/agent/result` | Agent | `X-Agent-Token`（client token） |
| Relay | POST | `/api/agent/relay/exec` | 连接插件 | `X-Relay-Token` |
| Relay | POST | `/api/agent/relay/put` | 连接插件 | `X-Relay-Token` |
| Relay | POST | `/api/agent/relay/fetch` | 连接插件 | `X-Relay-Token` |

网关前缀：

- 管理面：与现有 CMDB 接口一致，前端实际请求 `{网关}/cmdb/api/cmdb/agent/...`，自动带 `Authorization`、`Tenant-Id`。
- Agent / Relay 面：`{server}/agent/api/agent/...`（`server` 形如 `https://192.168.1.150/sjxy-console`），Shiro 匿名放行，由控制器按 token 头自校验。

扫描 / 安装 / 回滚 / 连通性 / 采集：**没有任何 Agent 专用接口**，前端继续调现有任务接口，后端在收口点自动按绑定表选通道。

---

## 1. 通用约定

### 1.1 错误响应

管理面 400：

```json
{ "errorCode": "HOST_ALREADY_BOUND", "message": "该资产已绑定 Agent，请先解绑" }
```

参数校验失败走平台统一 Problem 格式（无 `errorCode`），前端取 `errorCode ?? detail`：

```json
{ "title": "Bad Request", "status": 400, "detail": "clientId 和 hostId/newHost 不能同时为空" }
```

Agent 面 401：

```json
{ "ok": false, "errorCode": "NEED_REENROLL", "message": "…", "retryable": false }
```

Relay 面：HTTP 恒 200，错误放在 body 的 `errorCode`（见 §4）。

### 1.2 错误码（共 11 个）

| errorCode | 面 | 含义 | 前端文案建议 |
|---|---|---|---|
| `AGENT_OFFLINE` | 管理 / 任务下发 / Relay | Agent 未注册或 90s 内无心跳 | Agent 离线，暂不可执行 |
| `AGENT_AUTH_FAILED` | Agent / Relay | 缺 token 或 relay token 不匹配 | — |
| `NEED_REENROLL` | Agent | clientToken 已作废（解绑 / 吊销），须用新凭证重新纳管 | — |
| `ENROLL_TOKEN_INVALID` | Agent | 纳管凭证无效；响应体附 `reason: expired \| exhausted \| revoked \| unknown` | — |
| `DISPATCH_TIMEOUT` | Relay | 单条指令超过 `agent.dispatch.timeout-sec` 未回结果 | 下发/执行超时 |
| `COMMAND_FAILED` | Relay | 指令送达但执行异常 | 主机命令执行失败 |
| `RELAY_UNAVAILABLE` | Relay | 通道内部中断 | 通道不可达 |
| `CLIENT_ALREADY_BOUND` | 管理 | 该 Agent 已绑定其他资产 | 该 Agent 已绑定其他资产 |
| `HOST_ALREADY_BOUND` | 管理 | 目标资产已绑定 Agent | 该资产已绑定 Agent，请先解绑 |
| `HOST_NOT_FOUND` | 管理 | 资产不存在 / 不属于当前租户 / 无有效绑定 | 资产不存在 |
| `PLATFORM_SELF` | 管理 / 任务下发 | 目标或 Agent 所在机是平台自身 | 平台节点不能作为被纳管对象 |

任务下发被收口点拒绝时（目标含平台自身地址），任务接口返回的失败信息以 `[AgentRoute]` 开头、多行、逐台列出：

```
[AgentRoute] 本次下发已阻断，1 台目标不可执行：
- [PLATFORM_SELF] ciId=ci-2002 (oplus-node) 目标 192.168.1.150 是平台自身地址
```

前端保留换行原样展示。Agent 离线**不**整批阻断：与 SSH 主机不可达同一口径，该主机照常进入运行，在运行记录里以 `AGENT_OFFLINE` 单独失败，其余主机不受影响（前端按 `host-info.agentStatus` 预先禁按钮即可）。

### 1.3 时间格式

管理面所有时间字段 `yyyy-MM-dd HH:mm:ss`（GMT+8）。

---

## 2. 管理面

### 2.1 生成纳管凭证

`POST /api/cmdb/agent/enroll-tokens`

请求（均可选）：

```json
{ "targetOs": "windows", "ttlMinutes": 1440, "maxUses": 0, "remark": "财务部台式机", "hostId": null }
```

| 字段 | 默认 | 说明 |
|---|---|---|
| `targetOs` | `linux` | `linux` / `windows`，决定引导脚本形态；传了 `hostId` 时按资产类型自动取 |
| `ttlMinutes` | 1440（24h） | 有效期 |
| `maxUses` | 0 | 0 = 有效期内不限次；传 1 即一次性；传了 `hostId` 强制 1 |
| `remark` | — | 备注 |
| `hostId` | — | **预绑定资产**。凭此凭证注册的 Agent 直接绑到该资产，不进待处理列表。资产不存在 → 400 `HOST_NOT_FOUND`，已绑 Agent → `HOST_ALREADY_BOUND`，平台自身 → `PLATFORM_SELF` |

响应：

```json
{
  "id": "tok-abc",
  "token": "H8s...Uk",
  "targetOs": "windows",
  "hostId": null,
  "clientId": "kops-60591a2c554a",
  "status": "active",
  "maxUses": 0,
  "usedCount": 0,
  "remainingUses": null,
  "expiresAt": "2026-09-11 11:40:00",
  "bootstrapUrl": "https://192.168.1.150/sjxy-console/agent/api/agent/i/H8s...Uk",
  "installCommand": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"[Net.ServicePointManager]::ServerCertificateValidationCallback={$true};[Net.ServicePointManager]::SecurityProtocol='Tls12';iex((New-Object Net.WebClient).DownloadString('https://192.168.1.150/sjxy-console/agent/api/agent/i/H8s...Uk'))\""
}
```

- `token` 明文仅本次返回。
- `remainingUses`：`maxUses=0` 时为 `null`（不限），否则 `maxUses - usedCount`。
- `installCommand` **一行**，在目标机以 root / 管理员执行。Linux：`curl -fsSLk -o /tmp/koreops-i.sh '<bootstrapUrl>' && sudo bash /tmp/koreops-i.sh`；Windows 如上，cmd 或 PowerShell 都能贴。命令本身只含地址，server / token / CA 引导 / http 回退全在平台渲染的引导脚本里（见 3.0）。原 `agent.install.command-template.*` 配置已删。
- `clientId`：**平台签发的 Agent 身份**（`kops-` + 12 位十六进制），单次凭证（`maxUses=1`，含所有预绑定凭证）才有，多次凭证为 `null`。它随引导脚本以 `--client-id` / `-ClientId` 传给安装器；Agent 注册时上报的 `clientId` 必须与之一致，否则拒绝且凭证不消耗。管控机 IP 不固定，平台认这个身份不认 IP。资产页可在装机前就展示"这台机器的 Agent 身份"。
- 注册后的绑定行为见 3.1：预绑定 → 直接绑；通用凭证 → 自动建档；判不了的进 2.3。

### 2.1a 平台推装 Agent（已录资产，人不登目标机）

`POST /api/cmdb/agent/install`　请求 `{ "hostIds": ["h1", "h2"] }`

```json
{ "runId": "run-xxx", "submitted": ["h1"], "skipped": { "h2": "已由 Agent 纳管（cid…），如需重装先解绑" } }
```

- 每台签一张一次性预绑定凭证，作为主机变量 `agent_token / agent_server / agent_os` 传给 `sjxy/sjxy-secops/sjxy-agent-install/site.yml`，用资产现有 SSH / WinRM 凭据登上去执行引导脚本；Agent 注册即绑定。
- Linux / Windows 混选合成一个 run。跳过原因：资产不存在 / 不属当前租户、平台自身地址、已被 Agent 纳管。
- 结果看两处：`GET /host-info` 的 `agentStatus` 变 `online`；运行记录（`runId`）里 `ok_hosts / failed_hosts{hostId: 原因}`，不可达（凭据/网络）与安装失败（rc + 输出）分列。
- `POST /api/cmdb/agent/install/callback`：workflow 回调，前端不用管。

### 2.2 撤销凭证

`DELETE /api/cmdb/agent/enroll-tokens/{id}` → `200 "ok"`。已用该凭证注册的 Agent 不受影响（它们持有的是 clientToken）。

### 2.3 待处理 Agent（自动绑定判不了的才会出现在这里）

`GET /api/cmdb/agent/pending?tokenId=tok-abc`

`tokenId` 可选；传了则响应带 `token` 段。建议轮询间隔 3~5s。

自 2026-09-10 第二批起，注册时平台先自动绑定（预绑定凭证 → 直接绑该资产；通用凭证 → 自动建档并绑）。平台**不按 IP 匹配已有资产**（管控机 IP 不固定）。**列表里只剩**：预绑定的资产已被别的 Agent 占用（先解绑再绑）、自动建档失败、或 `agent.enroll.auto-create` 被关掉。正常纳管不需要打开此页。

```json
{
  "token": {
    "id": "tok-abc",
    "status": "active",
    "remainingUses": null,
    "expiresAt": "2026-09-11 11:40:00"
  },
  "agents": [
    {
      "clientId": "desk-189a1dcdb79e",
      "hostname": "WIN-IBP0LHHQ0CD",
      "ip": "192.168.1.93",
      "os": "Windows 10.0.17763",
      "agentPlatform": "windows",
      "agentVersion": "2.6.1",
      "capabilities": "scan,patch,rollback,exec,put,fetch",
      "agentStatus": "online",
      "registeredAt": "2026-09-10 11:35:02",
      "lastSeenAt": "2026-09-10 11:39:50",
      "matchedHosts": [
        { "hostId": "ci-3001", "hostname": "WIN-IBP0LHHQ0CD", "ip": "192.168.1.93", "ciType": "windows", "matchedBy": "ip,hostname" }
      ]
    }
  ]
}
```

- `agents` = 当前租户下已注册、`hostId` 为空的 Agent（含所绑资产已被删除的）。
- `matchedHosts`：按 `ip` 或 `hostname` 命中的已有资产，**只是候选**，后端不做任何自动绑定。0 条 → 前端默认选"新建资产"；≥1 条 → 让管理员挑，或仍可选新建。
- `token.status` 非 `active`、或 `remainingUses === 0` 时，前端停止轮询并提示"凭证已不可用，请回上一步重新生成"。

### 2.4 绑定（建档 + 绑定一步完成）

`POST /api/cmdb/agent/bind`

三种用法，`hostId` 与 `newHost` **二选一**：

**a. 本机模式，绑到已有资产**

```json
{ "clientId": "desk-189a1dcdb79e", "hostId": "ci-3001" }
```

**b. 本机模式，新建资产**

```json
{ "clientId": "desk-189a1dcdb79e", "newHost": {} }
```

`newHost` 可全空：后端用该 Agent 上报的 `hostname / ip / os` 建资产，CI 类型按 `agentPlatform` 选 `linux` / `windows`。也可显式覆盖：

```json
{ "clientId": "desk-189a1dcdb79e",
  "newHost": { "hostname": "PC-FIN-01", "ip": "192.168.1.93", "ciType": "windows", "groupIds": ["g-1"] } }
```

**c. 跳板模式（登记由该 Agent 代管的内网主机）**

```json
{ "clientId": "desk-95-001", "mode": "gateway", "targetIp": "10.0.0.5",
  "newHost": { "hostname": "db-01", "ip": "10.0.0.5", "ciType": "linux" } }
```

或 `hostId` 指向已有资产。`targetIp` 必填；一台 Agent 可 gateway 多台。

| 字段 | 必填 | 说明 |
|---|---|---|
| `clientId` | 是 | 来自 `pending.agents[].clientId`，或已绑定 Agent 的 `host-info.agentClientId`（gateway 时） |
| `hostId` | 与 `newHost` 二选一 | 已有资产 |
| `newHost` | 与 `hostId` 二选一 | `{hostname?, ip?, ciType?, osVersion?, osDistro?, groupIds?}` |
| `mode` | 否 | `local`（默认）/ `gateway` |
| `targetIp` | gateway 必填 | 内网目标地址 |

响应 200：

```json
{
  "hostId": "ci-3001",
  "clientId": "desk-189a1dcdb79e",
  "agentMode": "local",
  "targetIp": null,
  "agentStatus": "online",
  "hostCreated": true
}
```

后端在这一步做的事：校验（资产存在且属当前租户；资产未绑其他 Agent；local 时该 client 未绑其他资产；资产 IP / Agent IP / targetIp 都不是平台地址）→ 可选建资产 → 写绑定行 → 审计。**不再写 `cmdb_ansible_config` / `cmdb_automation`。**

错误：`HOST_ALREADY_BOUND` / `CLIENT_ALREADY_BOUND` / `HOST_NOT_FOUND` / `AGENT_OFFLINE`（该 clientId 未注册）/ `PLATFORM_SELF`。

绑定成功后前端跳该主机详情。绑定界面应把 Agent 上报的 `hostname / ip` 与所选资产的 `hostname / ip` 并排显示供核对——这是防"绑错机器"的正确位置。

### 2.5 解绑

`POST /api/cmdb/agent/unbind`

```json
{ "hostId": "ci-3001" }
```

→ `200 "ok"`。local 绑定：摘掉 `hostId`，Agent 回到 `pending` 列表，可重新绑定；gateway 绑定：删除该行。均写审计。

### 2.6 主机 Agent 信息（列表 / 详情 / 任务详情富化）

`GET /api/cmdb/agent/host-info?hostIds=id1,id2,id3`

返回数组，与入参顺序一致：

```json
[
  {
    "hostId": "ci-3001",
    "connectionType": "koreops_agent",
    "agentClientId": "desk-189a1dcdb79e",
    "agentMode": "local",
    "targetIp": null,
    "agentStatus": "online",
    "agentVersion": "2.6.1",
    "capabilities": "scan,patch,rollback,exec,put,fetch",
    "os": "Windows 10.0.17763",
    "agentPlatform": "windows",
    "cmdbIp": "192.168.1.93",
    "agentIp": "192.168.1.93",
    "ipMismatch": false,
    "lastSeenAt": "2026-09-10 11:39:50"
  },
  { "hostId": "ci-1002", "connectionType": "ssh", "cmdbIp": "192.168.1.94" }
]
```

| 字段 | 说明 |
|---|---|
| `connectionType` | `ssh` / `koreops_agent`（有效绑定即为后者；WinRM 主机也是 `ssh`） |
| `agentStatus` | `online` / `offline`，由内存 hub 判定；SSH 主机为 `null` |
| `agentMode` / `targetIp` | `local` / `gateway`；gateway 时 `targetIp` 为代管目标 |
| `agentPlatform` | `windows` / `linux` / `unknown`，按平台区分按钮用这个，不要解析 `os` |
| `cmdbIp` | 资产档案主 IP（SSH 主机也返回） |
| `agentIp` | local 时 Agent 最近上报 IP；gateway 时 = `targetIp`。即命令实际落点 |
| `ipMismatch` | `cmdbIp` 与 `agentIp` 都有值且不同。**仅提示**，不阻断下发 |

按钮规则（不变）：SSH 主机沿用现有逻辑；Agent 主机 `agentStatus==online` 且 `capabilities` 含对应能力（scan / patch / rollback）才可用，否则禁用并给出原因；批量选择时展示"可执行 N 台 / 不可执行 M 台 + 原因"，不静默跳过。

---

## 3. Agent 面（客户端协议，与 agent.py 2.6.1 完全兼容）

BASE = `{server}/agent/api/agent`。JSON 一律 camelCase，头 `X-Agent-Token`。

### 3.0 引导脚本（一行安装命令背后拉的东西）

`GET {BASE}/i/{enrollmentToken}` → `200 text/plain`（`Cache-Control: no-store`）

- 凭证 `targetOs=linux`：bash 脚本，内容为 `curl -fsSLk -o /tmp/koreops-install.sh {server}/agent/install.sh && exec bash /tmp/koreops-install.sh --token … --server …`。
- `targetOs=windows`：PowerShell 脚本，WebClient + TLS1.2 + 忽略自签证书，下载 `{server}/agent/wheels/win/install.ps1` 后以 `-Token -Server` 执行。
- 只校验凭证仍 `active`，**不消耗次数**（消耗发生在随后的 register）。无效/过期/用尽/吊销一律 `404` 纯文本，不区分原因。
- 凭证会出现在 URL 与 nginx 访问日志里：凭证本就是短期、一次性（预绑定）或限次的，保持 `ttlMinutes` 短即可。

### 3.1 注册 / 重连

`POST {BASE}/register`，头 `X-Agent-Token: <enrollment token 或 client token>`

```json
{ "clientId": "desk-189a1dcdb79e", "hostname": "WIN-IBP0LHHQ0CD", "ip": "192.168.1.93",
  "os": "Windows 10.0.17763", "agentVersion": "2.6.1", "capabilities": "scan,patch,rollback,exec,put,fetch" }
```

| 情形 | 结果 |
|---|---|
| clientId 未见过 + 有效 enrollment token | 先校验身份：凭证带 `clientId`（单次凭证）而上报的不一致 → `ENROLL_TOKEN_INVALID [reason=identity_mismatch]`，凭证不消耗。通过后创建绑定记录并**自动绑定**（凭证带 hostId → 绑该资产；否则 `agent.enroll.auto-create` → 建档后绑；资产被占 → 留待处理），`200 {ok:true, clientToken:"…"}`；Agent 落盘 clientToken。自动绑定失败不影响注册。**不按 IP 匹配** |
| clientId 已知 + client token 匹配 | 刷新元数据与在线，`200 {ok:true}` |
| clientId 已知 + token 不匹配，但它是一张有效 enrollment token | **就地换发** 新 clientToken，`200 {ok:true, clientToken:"…"}`（"踢下线 → 重新签发 → 不登机重装"路径） |
| token 无效 | `401 {ok:false, errorCode, message, retryable:false, reason?}` |
| 上报 IP 是平台地址 | `401 errorCode=PLATFORM_SELF` |

### 3.2 长轮询取指令（兼心跳）

`GET {BASE}/poll?clientId=…&ip=…&hostname=…`

- `ip` / `hostname` 可选；变化时刷新绑定行，供 DHCP 场景展示。
- 挂起最长 30s；有指令 `200` 返回指令体，无指令 `204`。
- `401` 同 §3.1 错误体；Agent 据 `retryable` 决定退避策略。

指令体：

```json
{ "cmdId": "uuid", "type": "exec", "target": null, "shell": "powershell", "cmd": "…" }
{ "cmdId": "uuid", "type": "put",   "target": "10.0.0.5", "dest": "/tmp/x", "content": "<base64>" }
{ "cmdId": "uuid", "type": "fetch", "target": null, "src": "/tmp/result.json" }
```

- `target` 空 = 本机执行；非空 = gateway 模式，Agent 用本地 `targets.json` 里的凭据经 SSH（Linux）/ WinRM（Windows）转发。
- `shell`：`sh` / `powershell` / `cmd`，由平台按该 Agent 的 `os` 判定填入。

### 3.3 回传结果

`POST {BASE}/result?clientId=…`

```json
{ "cmdId": "uuid", "rc": 0, "stdout": "…", "stderr": "…", "content": "<base64，仅 fetch>" }
```

→ 恒 `200 "ok"`（含重复 / 未知 cmdId，幂等由 Agent 侧 `cmd-history.json` 保证）。超时映射 `rc=124`。

---

## 4. Relay 面（executor 连接插件 → 平台，内部）

头 `X-Relay-Token: <agent.relay.internal-token>`；配置为空时不校验（仅开发）。三个接口均**同步等待** Agent 结果，上限 `agent.dispatch.timeout-sec`（默认 120s）；插件 HTTP 超时 = 该值 + 10s。

| 路径 | 请求体 | 说明 |
|---|---|---|
| `POST /relay/exec` | `{clientId, target?, cmd, shell?}` | `shell` 缺省时平台按绑定 os 判定 |
| `POST /relay/put` | `{clientId, target?, dest, content(base64)}` | |
| `POST /relay/fetch` | `{clientId, target?, src}` | 结果在响应 `content` |

响应（HTTP 恒 200）：

```json
{ "rc": 0, "stdout": "…", "stderr": "…", "content": null, "errorCode": null, "message": null }
```

`errorCode` 非空表示通道层失败（`AGENT_OFFLINE` / `DISPATCH_TIMEOUT` / `COMMAND_FAILED` / `RELAY_UNAVAILABLE` / `AGENT_AUTH_FAILED`），此时 `rc=-1`，插件把它当该主机 UNREACHABLE，ansible 走正常失败分支。

---

## 5. inventory 连接参数（派生，不落库）

任务下发时，收口点 `CmdbAutomationServiceImpl` 对每台有有效绑定的主机在内存里生成：

```
ansible_connection=koreops_agent koreops_agent_client_id=desk-95-001 koreops_relay_url=https://192.168.1.150/sjxy-console/agent/api/agent
ansible_connection=koreops_agent koreops_agent_client_id=desk-95-001 koreops_relay_url=… koreops_agent_target=10.0.0.5   # gateway
```

生成前一条硬规则：目标 / Agent 所在机 / targetIp 任一为平台地址 → `PLATFORM_SELF`，整批拒绝，错误信息格式见 §1.2。Agent 不在线只记 warn 并照常下发，由插件按 `AGENT_OFFLINE` 单台失败（否则定时连通性检测这种全租户一批的作业会因一台离线而整批失败）。

executor 节点插件 `koreops_agent.py` 读取 `koreops_*`（过渡期兼容 `oplus_*`），relay token 从进程环境变量 `AGENT_RELAY_INTERNAL_TOKEN` 取，不进 inventory。

---

## 6. 与现行接口的对照

| 现行 | 精简后 | 前端动作 |
|---|---|---|
| `POST /agent/enrollment-token` | `POST /agent/enroll-tokens`，请求多 `targetOs`（已有），响应删 `installCommandLinuxHttps / LinuxHttp / LinuxCa / Windows` 四字段 | 只读 `installCommand` |
| `GET /agent/enrollment-token/{id}` | **删除**，并入 `pending?tokenId=` | 等待页只轮询 `pending` |
| `DELETE /agent/enrollment-token/{id}` | `DELETE /agent/enroll-tokens/{id}` | 改路径 |
| `GET /agent/pending`（数组） | `GET /agent/pending?tokenId=`（对象 `{token, agents}`，agents 元素多 `agentPlatform / matchedHosts / lastSeenAt`） | 改解析；用 `matchedHosts` 渲染候选资产 |
| `POST /agent/bind {clientId, hostId}` | 同路径，增 `newHost / mode / targetIp` | 向导第 3 步加"新建资产"，不再跳"新增主机" |
| `POST /agent/bind-gateway` | **删除**，用 `bind` + `mode=gateway` | 改调用 |
| `POST /agent/unbind` | 不变 | — |
| `GET /agent/host-info` | 字段：`lastReportedIp / lastReportedAt` → `agentIp`；`agentStatus` 无 `unknown`；其余不变 | 改字段名；下线"同步资产 IP"按钮 |
| `POST /agent/sync-ip` | **删除** | 下线入口；资产 IP 由 CMDB 资产编辑维护，或开 `agent.dhcp.auto-sync-asset-ip` |
| `GET /agent/route-mismatch` | **删除** | 下线"接入体检"页 |
| 任务失败信息 `[AgentRouteMismatch]` 四种 kind | `[AgentRoute]` 一种（`PLATFORM_SELF`）；离线 Agent 在运行记录里单台 `AGENT_OFFLINE` | 同样多行原样展示 |
| 错误码 `ENROLL_TOKEN_EXPIRED / EXHAUSTED`、`PLATFORM_SELF_BIND`、`CAPABILITY_UNSUPPORTED`、`RESULT_INGEST_FAILED`、`ROLLBACK_*` | 见 §1.2 | 文案映射表按 §1.2 收缩 |
| Agent 面 `register / poll / result`、Relay 面 `relay/*` | **不变** | Agent 2.6.1 与现有连接插件无需升级 |

## 7. 第二批（操作流程精简，2026-09-10）新增 / 变化

| 接口 | 说明 | 前端动作 |
|---|---|---|
| `POST /cmdb/agent/install {hostIds}` | 平台推装 Agent（§2.1a） | 主机列表加"安装 Agent"，轮询 `host-info` |
| `POST /cmdb/agent/enroll-tokens` 增 `hostId`；响应增 `bootstrapUrl / hostId`，`installCommand` 变一行 | 预绑定凭证；一行安装命令（§2.1） | 资产行"生成本机安装命令"；凭证页只留一个复制按钮 |
| `GET /agent/i/{token}` | 引导脚本（§3.0），目标机拉 | 无 |
| `GET /cmdb/agent/pending` 语义 | 只剩自动绑定判不了的（§2.3） | 页面改名"待处理"，非主路径 |
| `POST /secops/patch/scan {hostIds}` → `{windowsRunId, linuxRunIds[], windowsHosts, linuxHosts}` | Linux/Windows 统一扫描入口，后端按 ci_type 分流 | 只留一个"扫描"按钮 |
| `POST /secops/v2/patch/task/{id}/run` → 任务实体 | 一键串行：预检查 → 安装/回滚 → 重启 → 校验；需重启时停在 `INSTALL_DONE/ROLLBACK_DONE` 等 `restart/confirm`，确认即执行（不用再点 `restart/execute`），回调后自动校验。停在哪就从哪接着跑 | 任务详情加"一键执行"；停点按 `restart/options` 弹确认 |
| `POST /secops/v2/patch/reboot-host` | 不变；完成后自动触发该主机补丁重扫 | 去掉"请手动扫描"提示 |
| 配置 `agent.install.command-template.linux/windows` | **删除** | — |
| 配置 `agent.enroll.auto-create`（默认 true） | 通用凭证自动建档开关；不按 IP 匹配已有资产 | — |
| `enroll-tokens` 响应 `clientId` | 平台签发的 Agent 身份（单次凭证），引导脚本自动带 `--client-id` | 资产页可展示；无需前端传 |
