# Windows 补丁管理业务说明书

## 一、这份文档讲什么

本文档面向业务管理人员，用通俗的方式说明 oplus 平台如何利用微软 WSUS 技术来管理 Windows 服务器的补丁。读完本文后，您将了解：

- 补丁管理解决了什么问题
- 整个系统由哪些角色协作完成
- 补丁扫描、安装、回滚各自是怎么工作的
- 日常操作的完整流程

---

## 二、为什么要做补丁管理

Windows 操作系统会不断发现安全漏洞。微软每月发布安全更新（俗称"补丁"）来修复这些漏洞。每个补丁有一个唯一编号，以 **KB** 开头（例如 KB5034127）。

如果服务器长期不打补丁，就像房屋门窗有裂缝却不修补，容易被攻击者利用。补丁管理的目标就是：

> **及时发现哪些服务器缺少哪些补丁，按需安装，并在出问题时能够回退。**

### 离线环境的特殊挑战

很多企业的核心服务器部署在**隔离网络（断网环境）**中，无法直接连接互联网下载补丁。这时候就需要 **WSUS（Windows Server Update Services）** 来充当一个"本地补丁仓库"——先在有网环境下载好补丁，再搬运到离线环境中使用。

---

## 三、系统中的"角色"——谁做什么

整个补丁管理流程涉及四个关键角色，可以用一个"快递取件"的比喻来理解：

```
┌──────────┐      ┌──────────┐      ┌──────────┐      ┌──────────┐
│  操作人员  │      │ oplus平台 │      │  调度引擎  │      │ Windows  │
│ （下单方） │─────▶│ （客服中心）│─────▶│ （快递员） │─────▶│  服务器  │
│          │      │          │      │  (JAO)   │      │ （收件方） │
└──────────┘      └──────────┘      └──────────┘      └──────────┘
                                                           │
                                                           ▼
                                                      ┌──────────┐
                                                      │ WSUS服务器│
                                                      │（补丁仓库）│
                                                      └──────────┘
```

| 角色 | 比喻 | 职责 |
|------|------|------|
| **操作人员** | 下单方 | 在平台上选择要操作的服务器，发起扫描/安装/回滚 |
| **oplus 平台** | 客服中心 | 接收请求、记录任务、分派工作、汇总结果、生成报告 |
| **JAO 调度引擎** | 快递员 | 把具体的操作指令送达到每一台 Windows 服务器上执行 |
| **Windows 服务器** | 收件方 | 接收指令后，在本机上实际执行扫描/安装/卸载操作 |
| **WSUS 服务器** | 补丁仓库 | 存放所有可用补丁，供 Windows 服务器下载安装 |

---

## 四、补丁扫描——"体检"

### 4.1 扫描是什么

补丁扫描就像给服务器做一次"体检"，检查哪些补丁已经安装了、哪些还缺着。

### 4.2 扫描的完整过程

```
                    操作人员                  oplus 平台               Windows 服务器
                      │                         │                         │
                ①  选择服务器               　　 │                         │
                   点击"扫描"                   │                         │
                      │                         │                         │
                      ├────── 发起扫描请求 ─────▶│                         │
                      │                         │                         │
                      │                    ② 创建扫描任务                  │
                      │                      记录到数据库                  │
                      │                         │                         │
                      │                    ③ 派发指令给                    │
                      │                      调度引擎(JAO)                │
                      │                         │                         │
                      │                         ├──── 远程执行扫描脚本 ───▶│
                      │                         │                         │
                      │                         │                    ④ 服务器本地检查
                      │                         │                      询问 WSUS 仓库：
                      │                         │                      "我缺哪些补丁？"
                      │                         │                      "我已装哪些补丁？"
                      │                         │                         │
                      │                         │◀─── 返回扫描结果 ────────┤
                      │                         │                         │
                      │                    ⑤ 解析结果                     │
                      │                      更新每台服务器的              │
                      │                      补丁清单                     │
                      │                         │                         │
                      │◀──── 扫描完成，可查看 ───┤                         │
                      │                         │                         │
```

**步骤说明：**

1. **操作人员选择服务器并点击扫描** —— 可以一次选择多台服务器批量扫描。

2. **平台创建扫描任务** —— 系统记录"谁在什么时间对哪些服务器发起了扫描"，任务状态变为"执行中"。

3. **平台派发指令** —— 平台通过调度引擎（JAO），将扫描指令送到每一台被选中的 Windows 服务器上。

4. **服务器本地执行扫描** —— 每台服务器上会运行一个检查脚本，该脚本通过 Windows 系统内置的"更新代理"，向 WSUS 仓库查询：
   - 有哪些补丁我还没装？（**缺失补丁**）
   - 有哪些补丁我已经装了？（**已安装补丁**）
   
   同时收集每个补丁的编号（KB号）、名称、严重程度等信息。

5. **平台汇总结果** —— 收到每台服务器的扫描报告后，平台会：
   - 记录每台服务器缺少哪些补丁
   - 按严重程度分类统计（严重 / 重要 / 中等 / 低危）
   - 如果某个补丁上次扫描时还缺着，这次却不在缺失列表中了，说明有人在平台之外手动装上了，系统会自动将其标记为"已安装"

### 4.3 扫描结果怎么看

扫描完成后，操作人员可以看到两级信息：

**第一级：主机概览**（每台服务器一行）

| 服务器IP | 操作系统 | 缺失补丁总数 | 严重 | 重要 | 中等 | 低危 | 已安装 | 最后扫描时间 |
|----------|---------|-------------|------|------|------|------|--------|------------|
| 10.1.0.1 | Windows Server 2022 | 5 | 2 | 2 | 1 | 0 | 120 | 2026-04-14 10:30 |
| 10.1.0.2 | Windows Server 2019 | 3 | 1 | 1 | 1 | 0 | 95 | 2026-04-14 10:32 |

**第二级：补丁明细**（点击某台服务器后查看）

| KB编号 | 补丁名称 | 严重级别 | 分类 | 状态 |
|--------|---------|---------|------|------|
| KB5034127 | 2024-01 Windows Server 2022 累积更新 | 严重 | 安全更新 | 缺失 |
| KB5033909 | 2023-12 Windows Server 2022 安全更新 | 重要 | 安全更新 | 已安装 |

### 4.4 严重级别是什么意思

| 级别 | 含义 | 建议处理优先级 |
|------|------|--------------|
| **严重 (Critical)** | 可被远程利用的高危漏洞，可能导致系统被完全控制 | 最高优先级，应尽快安装 |
| **重要 (Important)** | 可能导致数据泄露或权限提升的漏洞 | 高优先级，建议尽快安装 |
| **中等 (Moderate)** | 利用难度较高或影响有限的漏洞 | 按计划安装 |
| **低危 (Low)** | 影响很小的问题 | 可安排在维护窗口统一处理 |

---

## 五、补丁安装——"打补丁"

### 5.1 安装是什么

补丁安装就是把缺失的补丁实际部署到服务器上，让漏洞得到修复。

### 5.2 安装的完整过程

```
                    操作人员                  oplus 平台               Windows 服务器
                      │                         │                         │
                ①  从扫描结果中               　│                         │
                   勾选要安装的补丁             │                         │
                   点击"安装"                  │                         │
                      │                         │                         │
                      ├────── 发起安装请求 ─────▶│                         │
                      │                         │                         │
                      │                    ② 创建安装任务                  │
                      │                      按服务器分组                  │
                      │                      整理 KB 清单                 │
                      │                      补丁状态→"安装中"             │
                      │                         │                         │
                      │                    ③ 派发指令                     │
                      │                      告诉每台服务器               │
                      │                      "请安装这些 KB"              │
                      │                         │                         │
                      │                         ├──── 执行安装脚本 ──────▶│
                      │                         │                         │
                      │                         │                    ④ 服务器执行安装
                      │                         │                      · 从 WSUS 下载补丁文件
                      │                         │                      · 逐个安装补丁
                      │                         │                      · 记录每个补丁的结果
                      │                         │                         │
                      │                         │◀─── 返回安装结果 ────────┤
                      │                         │                         │
                      │                    ⑤ 更新状态                     │
                      │                      成功→"已安装"                │
                      │                      失败→"安装失败"              │
                      │                      记录安装日志                  │
                      │                         │                         │
                      │◀──── 安装完成，可查看 ───┤                         │
                      │                         │                         │
```

**步骤说明：**

1. **操作人员选择补丁** —— 在扫描结果中，勾选状态为"缺失"的补丁，可以跨多台服务器批量选择。还可以选择安装后是否自动重启服务器。

2. **平台整理任务** —— 系统自动按服务器分组，为每台服务器列出需要安装的 KB 清单。被选中的补丁状态从"缺失"变为"安装中"。

3. **平台派发指令** —— 通过调度引擎将安装指令和 KB 清单发送到各台服务器。

4. **服务器执行安装** —— 每台服务器上会：
   - 在 WSUS 仓库中搜索指定的 KB 补丁
   - 下载补丁文件到本地
   - 按顺序安装每个补丁
   - 记录每个补丁安装成功还是失败

5. **平台更新结果** —— 收到安装结果后：
   - 安装成功的补丁 → 状态变为"已安装"
   - 安装失败的补丁 → 状态变为"安装失败"，并记录失败原因
   - 每一条安装记录都会写入操作日志，方便追溯

### 5.3 安装失败的常见原因

| 原因 | 说明 | 建议处理 |
|------|------|---------|
| WSUS 上未审批 | 补丁虽然在仓库里，但管理员没有审批放行 | 联系 WSUS 管理员审批该补丁 |
| 磁盘空间不足 | C 盘空间不够存放补丁和安装临时文件 | 清理磁盘后重试 |
| 依赖缺失 | 需要先安装某个前置补丁 | 先安装前置补丁，再安装目标补丁 |
| 网络不通 | 服务器无法连接 WSUS 仓库 | 检查网络和防火墙配置 |

### 5.4 关于重启

- 部分补丁安装后需要**重启服务器**才能生效（尤其是系统核心组件的安全更新）
- 操作人员可以选择"安装后自动重启"或"安装后不重启"
- **建议**：在维护窗口期间安装补丁并重启，避免影响业务运行

---

## 六、补丁回滚——"后悔药"

### 6.1 回滚是什么

补丁回滚就是把已经安装的补丁卸载掉，让服务器恢复到安装之前的状态。当安装某个补丁后出现系统异常或应用兼容性问题时，可以通过回滚来恢复。

### 6.2 回滚的完整过程

```
                    操作人员                  oplus 平台               Windows 服务器
                      │                         │                         │
                ①  从安装日志中               　│                         │
                   选择要回滚的补丁             │                         │
                   点击"回滚"                  │                         │
                      │                         │                         │
                      ├────── 发起回滚请求 ─────▶│                         │
                      │                         │                         │
                      │                    ② 创建回滚任务                  │
                      │                      补丁状态→"回滚中"             │
                      │                         │                         │
                      │                    ③ 派发指令                     │
                      │                      "请卸载这些 KB"              │
                      │                         │                         │
                      │                         ├──── 执行回滚脚本 ──────▶│
                      │                         │                         │
                      │                         │                    ④ 服务器执行卸载
                      │                         │                      · 优先用标准方式卸载
                      │                         │                      · 标准方式不行就
                      │                         │                        用备用方式卸载
                      │                         │                         │
                      │                         │◀─── 返回回滚结果 ────────┤
                      │                         │                         │
                      │                    ⑤ 更新状态                     │
                      │                      成功→"缺失"（等待重装）      │
                      │                      失败→"回滚失败"              │
                      │                         │                         │
                      │◀──── 回滚完成，可查看 ───┤                         │
                      │                         │                         │
```

**步骤说明：**

1. **操作人员选择要回滚的补丁** —— 在"安装日志"中找到之前安装成功的补丁记录，选中后发起回滚。

2. **平台创建回滚任务** —— 被选中的补丁状态从"已安装"变为"回滚中"。

3. **平台派发指令** —— 将卸载指令发送到对应的服务器。

4. **服务器执行卸载** —— 系统会尝试两种卸载方式：
   - **标准方式**：通过 Windows 更新代理卸载（如果补丁支持卸载）
   - **备用方式**：如果标准方式不支持，系统会自动切换到 DISM 工具进行卸载

5. **平台更新结果** —— 
   - 回滚成功 → 补丁状态恢复为"缺失"（可在下次安装时重新安装）
   - 回滚失败 → 状态变为"回滚失败"

### 6.3 回滚的限制

并非所有补丁都能回滚，以下情况可能无法回滚：

| 情况 | 说明 |
|------|------|
| 补丁本身不支持卸载 | 微软部分安全更新设计为不可卸载，这是系统层面的限制 |
| 后续补丁有依赖 | 某些补丁被更新版本的补丁所依赖，需要先回滚新补丁 |
| 系统核心补丁 | 涉及系统启动组件的补丁通常不可卸载 |

> **建议**：安装补丁前先在测试环境验证，确认无兼容性问题后再推广到生产环境。

---

## 七、补丁的生命周期

每个补丁在系统中有一个清晰的状态流转过程：

```
                        ┌─────────────────────────────────────────┐
                        │            首次扫描发现                   │
                        └──────────────────┬──────────────────────┘
                                           ▼
                                    ┌─────────────┐
                         ┌─────────│    缺 失     │◀──────────┐
                         │         └──────┬───────┘           │
                         │                │                   │
                         │          发起安装                   │
                         │                │              回滚成功
                         │                ▼                   │
                         │         ┌─────────────┐     ┌─────────────┐
                         │         │   安装中     │     │   回滚中     │
                         │         └──┬──────┬───┘     └──────┬──────┘
                         │            │      │                │
                         │       安装成功  安装失败        回滚失败
                         │            │      │                │
                         │            ▼      ▼                ▼
                         │    ┌──────────┐ ┌───────────┐ ┌───────────┐
                         │    │  已安装   │ │ 安装失败  │ │ 回滚失败  │
                         │    └────┬─────┘ └───────────┘ └───────────┘
                         │         │
                         │    发起回滚
                         │         │
                         │         ▼
                         │   ┌─────────────┐
                         │   │   回滚中     │
                         │   └─────────────┘
                         │
                         │   再次扫描时，该 KB
                         │   已不在缺失列表中
                         │   （有人在平台外装了）
                         └────▶ 自动标记为"已安装"
```

**状态说明：**

| 状态 | 含义 | 可执行操作 |
|------|------|----------|
| **缺失** | 服务器缺少该补丁 | 可发起安装 |
| **安装中** | 补丁正在安装 | 等待完成 |
| **已安装** | 补丁已成功安装 | 可发起回滚 |
| **安装失败** | 补丁安装失败 | 可排查原因后重试安装 |
| **回滚中** | 补丁正在卸载 | 等待完成 |
| **回滚失败** | 补丁卸载失败 | 可排查原因后重试回滚 |

---

## 八、WSUS 补丁仓库是怎么工作的

### 8.1 WSUS 的角色

WSUS 的全称是 **Windows Server Update Services**（Windows 服务器更新服务），它是微软提供的免费企业级补丁管理工具。在离线环境中，WSUS 充当了一个"本地补丁商店"的角色：

```
┌──────────────────────┐                   ┌──────────────────────┐
│    有网环境            │                   │    离线环境（内网）     │
│                      │    搬运补丁文件     │                      │
│   微软官方更新源  ──────┼──────────────────▶│   WSUS 离线服务器     │
│       │              │   （通过U盘/光盘    │       │              │
│       ▼              │    等离线介质）     │       │              │
│   WSUS中转服务器      │                   │       ▼              │
│   （下载+导出）       │                   │   补丁审批放行         │
│                      │                   │       │              │
└──────────────────────┘                   │       ▼              │
                                           │   Windows 服务器      │
                                           │   从 WSUS 获取补丁    │
                                           │                      │
                                           └──────────────────────┘
```

### 8.2 补丁从微软到服务器的旅程

1. **微软发布补丁** —— 每月第二个周二（"补丁星期二"），微软发布安全更新

2. **有网 WSUS 同步** —— 在可以上网的环境中，WSUS 服务器从微软下载最新补丁

3. **导出搬运** —— 将补丁文件打包导出，通过移动介质搬运到离线环境

4. **离线 WSUS 导入** —— 在离线环境的 WSUS 服务器上导入补丁

5. **管理员审批** —— WSUS 管理员审核并批准需要部署的补丁（只有审批过的补丁才会被扫描到）

6. **服务器获取** —— Windows 服务器通过扫描发现可用补丁，通过安装从 WSUS 下载并安装

### 8.3 为什么扫描结果中没有某个补丁

如果您期望看到某个补丁但扫描结果中没有出现，可能的原因：

| 原因 | 排查方法 |
|------|---------|
| WSUS 未导入该补丁 | 联系 WSUS 管理员确认补丁是否已导入 |
| WSUS 未审批该补丁 | 联系 WSUS 管理员确认补丁是否已审批放行 |
| 补丁不适用于该系统 | 该补丁可能只适用于特定 Windows 版本 |
| 补丁已被更新版本替代 | 微软可能已发布更新的累积补丁替代了旧补丁 |

---

## 九、连通性测试——"开工前检查"

在进行扫描或安装之前，建议先对服务器做一次连通性测试。这相当于在施工前检查工具是否齐全、道路是否通畅：

| 检查项 | 通俗解释 | 不通过的影响 |
|--------|---------|-------------|
| **远程管理服务 (WinRM)** | 平台能否"远程控制"这台服务器 | 无法执行任何操作 |
| **脚本执行策略** | 服务器是否允许运行检查脚本 | 扫描/安装脚本会被拒绝执行 |
| **更新代理组件** | Windows 内置的补丁管理组件是否正常 | 无法查询和安装补丁 |
| **WSUS 连接** | 服务器能否连上补丁仓库 | 无法下载补丁 |
| **磁盘空间** | C 盘是否有足够空间（≥2GB） | 安装补丁时可能因空间不足而失败 |

> 所有检查项全部通过后，主机状态标记为 **PASS**，即可放心进行补丁操作。

---

## 十、日常操作流程总览

以下是日常补丁管理推荐的操作步骤：

```
  ┌─────────────────────────────────────────────────────────────────┐
  │                     日常补丁管理流程                              │
  │                                                                 │
  │   第一步                                                        │
  │   ┌──────────────┐                                              │
  │   │ 连通性测试    │  确保所有服务器状态正常                        │
  │   └──────┬───────┘                                              │
  │          │  全部 PASS                                           │
  │          ▼                                                      │
  │   第二步                                                        │
  │   ┌──────────────┐                                              │
  │   │  补丁扫描    │  扫描全部或指定服务器                          │
  │   └──────┬───────┘                                              │
  │          │  查看扫描结果                                         │
  │          ▼                                                      │
  │   第三步                                                        │
  │   ┌──────────────┐                                              │
  │   │  评估补丁    │  查看缺失补丁的严重级别和数量                   │
  │   │  制定计划    │  确定哪些补丁需要安装，安排维护窗口              │
  │   └──────┬───────┘                                              │
  │          │                                                      │
  │          ▼                                                      │
  │   第四步                                                        │
  │   ┌──────────────┐                                              │
  │   │  补丁安装    │  在维护窗口内执行补丁安装                      │
  │   └──────┬───────┘                                              │
  │          │  检查安装结果                                         │
  │          ▼                                                      │
  │   第五步                                                        │
  │   ┌──────────────┐                                              │
  │   │  验证确认    │  确认服务器和业务系统运行正常                   │
  │   └──────┬───────┘                                              │
  │          │                                                      │
  │          ├──── 正常 ──▶ 补丁管理完成，等待下次巡检               │
  │          │                                                      │
  │          └──── 异常 ──▶ 执行补丁回滚，恢复到之前状态              │
  │                                                                 │
  └─────────────────────────────────────────────────────────────────┘
```

### 操作要点

1. **定期扫描**：建议每月至少扫描一次，与微软补丁发布周期（每月第二个周二）保持同步
2. **分批安装**：不要一次性在所有服务器上安装，先在少量测试服务器上验证，再逐步推广
3. **优先处理"严重"级别**：Critical 和 Important 级别的补丁应优先安装
4. **选择维护窗口**：在业务低峰期执行安装和重启
5. **保留回滚能力**：安装后观察一段时间，确认无问题后再继续下一批

---

## 十一、导出报告

系统支持将补丁扫描结果导出为 Excel 报告，方便存档和上报。

**报告包含三个工作表：**

| 工作表 | 内容 |
|--------|------|
| **主机概况** | 每台服务器的补丁汇总（缺失数量、各级别数量、已安装数量） |
| **补丁明细** | 每台服务器每个补丁的详细信息（KB号、名称、级别、状态） |
| **统计概览** | 全局统计数据（总主机数、总缺失补丁数、各级别总数） |

导出时可以按条件筛选：
- 按服务器范围筛选
- 按严重级别筛选（如只导出"严重"级别的补丁）
- 按补丁状态筛选（如只导出"缺失"状态的补丁）

---

## 十二、CVE 漏洞与补丁的关系

### 12.1 什么是 CVE

**CVE（Common Vulnerabilities and Exposures）** 是全球通用的漏洞编号体系。每个已知的安全漏洞都有一个 CVE 编号（例如 CVE-2024-12345），就像每个漏洞的"身份证号"。

### 12.2 CVE 和 KB 补丁的关系

```
CVE-2024-12345（漏洞）
       │
       │  微软发布修复补丁
       ▼
KB5034127（补丁）
       │
       │  安装到服务器上
       ▼
漏洞修复
```

- **一个 CVE 漏洞**可能需要安装**一个或多个 KB 补丁**来修复
- **一个 KB 补丁**可能同时修复**多个 CVE 漏洞**
- 系统中的 CVE 漏洞数据来源于微软 MSRC（微软安全响应中心），与补丁扫描/安装是独立的功能模块
- 通过 **KB 编号**，可以关联查看某个补丁修复了哪些漏洞

---

## 十三、常见问题

### Q1：扫描需要多长时间？

通常 1-5 分钟/台服务器，取决于该服务器已安装的更新数量和 WSUS 仓库中的补丁数量。批量扫描多台服务器时是并行执行的。

### Q2：安装补丁会影响正在运行的业务吗？

- **安装过程中**：通常不会中断业务，但会占用一定的系统资源（CPU、磁盘IO）
- **安装完成后**：部分补丁需要重启服务器才能生效，重启期间业务会中断
- **建议**：在维护窗口期间操作，必要时通知业务方

### Q3：补丁安装失败了怎么办？

1. 查看安装日志中的错误信息
2. 根据错误原因排查（磁盘空间、WSUS 审批、网络连通性等）
3. 问题解决后可以重新发起安装

### Q4：回滚后补丁去哪了？

回滚成功后，补丁状态恢复为"缺失"。如果需要重新安装，可以再次从扫描结果中选择该补丁进行安装。

### Q5：怎么知道哪些补丁是最紧急需要安装的？

关注扫描结果中**严重级别为 "Critical（严重）"**的补丁。这些通常是高危漏洞的修复，应优先安装。
