/*! oplus-modules v1.0.0 */
const Linter=require("../linter"),NodeResolver=require("../resolver/node-resolver");async function compileConfig(e){const n=new Linter({resolver:new NodeResolver}),r=await n.resolveConfiguredRules(e),o=Object.keys(r).reduce(function(e,o){const l=r[o],{category:t}=n.parseRuleValue(l);return"off"!==t&&(e[o]=l),e},{});return`${`\nvar cache = {};\n\n/**\n * A resolver that caches rules and configuration as part of the bundle,\n * making them accessible in the browser.\n *\n * @param {Object} cache\n */\nfunction Resolver() {}\n\nResolver.prototype.resolveRule = function(pkg, ruleName) {\n\n  const rule = cache[pkg + '/' + ruleName];\n\n  if (!rule) {\n    throw new Error('cannot resolve rule <' + pkg + '/' + ruleName + '>');\n  }\n\n  return rule;\n};\n\nResolver.prototype.resolveConfig = function(pkg, configName) {\n  throw new Error(\n    'cannot resolve config <' + configName + '> in <' + pkg +'>'\n  );\n};\n\nvar resolver = new Resolver();\n\nvar rules = ${JSON.stringify(o,null,"  ")};\n\nvar config = {\n  rules: rules\n};\n\nvar bundle = {\n  resolver: resolver,\n  config: config\n};\n\nexport { resolver, config };\n\nexport default bundle;\n`}\n\n${Object.keys(o).map((e,r)=>{const{pkg:o,ruleName:l}=n.parseRuleName(e);return`\nimport rule_${r} from '${o}/rules/${l}';\ncache['${o}/${l}'] = rule_${r};`}).join("\n")}`}module.exports=compileConfig;